<head>
  <title>Vận đơn</title>
</head>
<header>
<h1>Vận đơn</h1>
<!-- <div>
<%= form_tag(vandons_path, method: :get) do %>
  <%= text_field_tag :term, params[:term] %>
  <%= submit_tag 'Tìm kiếm', name: nil %>
<% end %>
</div> -->

<div>
  

<%= form_tag(vandons_path, :method => "get", id: "search-form") do %>
<%= text_field_tag :search, params[:search], placeholder: "Tìm kiếm theo mã vận đơn" %>
<%= submit_tag "Tìm kiếm" %>
<% end %>


</div>
<ul class="actions">
<li>
<%= link_to "Thêm mới", new_vandon_path, class: "new" %>
</li>

</ul>
</header>
<div>
<table class="table-responsive"">
  <thead>
    <tr>
      <th>Vận đơn</th>
      <th>Ngay tao</th>
      <th> Ngay cap nhat</th>
    </tr>
  </thead>
  <tbody>
<% @vandons.each do |vandon| %>
<tr>
<td><%= link_to vandon.id, vandon %></td>
<td><%= vandon.created_at %></p></td>
<td><%= vandon.updated_at %></p></td>
<td><%= link_to t('.show', :default => t("helpers.links.show")),
                      vandon_path(vandon), class: "show" %></td>
<td><%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_vandon_path(vandon), class: "edit" %></td>

<!-- <td><%= link_to "Delete", vandons_path(@vandon),method: :delete,data: { confirm: "Are you sure you want to delete this table?" },class: "delete" %></td> -->
</tr>
<% end %>

</tbody>
</table>
</div>
