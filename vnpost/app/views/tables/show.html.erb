
<header>

<h1><%= @table.name %></h1>
<%= @table.mota %>
<ul class="actions">
<% if policy(@table).update? %>
<li><%= link_to "Edit", edit_table_path(@table),
class: "edit" %></li>
<% end %>
<% admins_only do %>
<li><%= link_to "Delete", admin_table_path(@table),method: :delete,data: { confirm: "Are you sure you want to delete this table?" },class: "delete" %></li>
<% end %>
<li><%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                tables_path, class: "cancel" %></li>
</ul>
</header>
<header>
<h2>Dich vu</h2>
<ul class="actions">
<li><%= link_to "New Dich vu", new_table_dichvu_path(@table),
class: "new" %></li>
</ul>
</header>
<!-- <ul id="dichvus">
<% @table.dichvus.each do |dichvu| %>
<li>
#<%= dichvu.id %> -
<%= link_to dichvu.tendv, [@table, dichvu] %>
</li>
<% end %>
</ul> -->

<div>
<table class="table-responsive"">
  <thead>
    <tr>
      <th>Dich vu</th>
      <th>Mô tả </th>
       <th>Ty le gia tri </th>
      <th> </th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
<% @table.dichvus.each do |dichvu| %>
<tr>
<td><%= link_to dichvu.tendv, [@table, dichvu] %></td>
<td><%= dichvu.mota %></p></td>
<td><%= dichvu.tylegiatri %></p></td>
<td><%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_table_dichvu_path(dichvu), class: "edit" %></td>

<td>
<% admins_only do %>

<td><%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      table_dichvu_path(dichvu), class: "delete" %></td><% end %>
</td>
</tr>
<% end %>
</tbody>
</table>
</div>
